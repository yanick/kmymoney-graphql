type Institution {
    name: String
}

type Account {
    id: String
    accountName: String
    institutionId: ID,
    institution: Institution,
    balance(date: String): Float
}

type Query {
    accounts: [Account],
    account(id: String!): Account,
}
